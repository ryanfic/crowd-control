using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BusStopGenerator : MonoBehaviour
{
    public GameObject BusTemplate;
    //private Vector3 VancouverCityCenterStationLocation = (1,1,1);//Skytrain, not bus
    // Start is called before the first frame update
    void Start()
    {
        //Instantiate(BusTemplate,VancouverCityCenterStationLocation,Quaternion.identity);
    }

}
